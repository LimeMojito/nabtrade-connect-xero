name: Version Update Check

on:
  workflow_dispatch:
  schedule:
    - cron: "25 18 * * 1"
    # at 6:25PM GMT or 4:25AM (+1) AEST on a Monday


jobs:
  version-update:
    uses: ./.github/workflows/oss-maven-patch-version.yml
    secrets: inherit
    permissions:
      id-token: write
      contents: write
      pull-requests: write
